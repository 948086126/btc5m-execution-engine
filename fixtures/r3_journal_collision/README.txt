V0 regression target:
A nested payload containing keys such as sequence, record_hash, journal_sequence, or journal_record_sha256
must never alter writer-owned Envelope.sequence / previous_hash / record_hash.
